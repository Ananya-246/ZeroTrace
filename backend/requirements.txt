# Core dependencies
cryptography>=41.0.0
reportlab>=4.0.0

# Linux-specific (only install on Linux)
pyudev>=0.24.0

# Windows-specific (only install on Windows)
pywin32>=306
wmi>=1.5.1


# Core dependencies
psutil>=5.9.0           # System and process utilities
cryptography>=41.0.0    # Cryptographic operations for certificates

# GUI dependencies (optional, for interface)
# tkinter is usually pre-installed with Python

# Utility dependencies
tqdm>=4.65.0           # Progress bars for CLI

# Development dependencies (optional)
pytest>=7.4.0          # Testing framework
black>=23.0.0          # Code formatting
flake8>=6.0.0          # Code linting

# Platform-specific requirements
pywin32>=306; sys_platform == 'win32'  # Windows-specific operations